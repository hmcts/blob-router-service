spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/reform-scan
      name: reform-scan
      aliases:
        app-insights-instrumentation-key: azure.application-insights.instrumentation-key
        storage-account-name: storage.accountName
        storage-account-primary-key:  storage.accountKey
        storage-account-secondary-key:  storage.accountSecondaryKey

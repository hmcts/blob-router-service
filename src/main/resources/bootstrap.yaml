spring:
  cloud:
    propertiesvolume:
      prefixed: false
      enabled: true
      paths: /mnt/secrets/reform-bulk-scan
      aliases:
        storage-account-name: storage.account_name
        storage-account-primary-key: storage.account-key
        storage-account-secondary-key: storage.account-secondary-key
